# This patch inject custom ENV settings to the manager container
# Used to set our default image locations
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        env:
        - name: WATCHER_API_IMAGE_URL_DEFAULT
          value: quay.io/tripleozedcentos9/openstack-watcher-api:current-tripleo
        - name: WATCHER_DECISION_ENGINE_IMAGE_URL_DEFAULT
          value: quay.io/tripleozedcentos9/openstack-watcher-decision-engine:current-tripleo
        - name: WATCHER_APPLIER_IMAGE_URL_DEFAULT
          value: quay.io/tripleozedcentos9/openstack-watcher-applier:current-tripleo
